# Google OAuth client ID for Login with Google
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Optional backend endpoint for AI Ask in the search modal.
# Keep GEMINI_API_KEY on the backend; do not put it in the noteX frontend.
VITE_NOTEX_AI_ENDPOINT=http://localhost:8787/api/ask
